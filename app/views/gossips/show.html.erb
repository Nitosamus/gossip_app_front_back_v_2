<h1>welcome</br>

	<%= @first_name %></br>
	Dans votre page de profil.</br>	


	<h2>votre notre c'est : <%= link_to @gossip.user.first_name, user_path(@gossip.user.id)%> </br>

		Voici votre meilleurs ptotin : </br><br>
	</h2>
		<div>
			<p>
				<ul>
					<li>Titre: <%= @gossip.title %></li>
					<li>contenue: <%= @gossip.content %></li>
					<li>auteur: <%=@gossip.user.last_name %><%= @gossip.user.first_name%></li>
					<li>ville: <%= @gossip.user.city.name %></li>
					 <p><u>commentaires</u>: <%= @comments.each do |i| %>
						<ul>
							<li>
								<%= i.content%> <br>
						<u>Auteur:</u> <%=i.user.first_name%> <br><br>
						<u>Auteur:</u> <%=i.id%> <br><br>

					<u><%= link_to "Modifier", edit_gossip_comment_path(params[:id], i.id)%></u><br>

							</li>
							<%end%>
           
						</ul>
							<%=form_for @comment do |f|%>
							<div class="form-group">
							<label>commenter</label><br>
							<%=f.text_field :content, class: "form-control", size: "70 *70", class:"btn btn-outline-info" %>
							<div class="form-group">
							<button type="submit" class="btn btn-success">ENVOYER</button> 
							<%end%>
							</div>
						</p>
					</li>
				</ul>
			</p>
		</div>

